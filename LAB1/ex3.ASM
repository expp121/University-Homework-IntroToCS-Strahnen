	CLO
	JMP START
	DB 0 ; VARIABLE A 
	DB 0 ; STORE 0 SOMEWHERE IN MEMORY (VARIABLE b)

START:
	MOV AL, [03]
	CMP AL , 3
	JZ INC

CONTINUE:
	ADD AL, 1
	MOV [03], AL
	CMP AL , 6
	JS START

	ADD AL, 30
	ADD BL, 30

	MOV CL ,41
	MOV [C0], CL
	MOV CL ,3D
	MOV [C1], CL
	MOV [C2], AL

	MOV CL , 42
	MOV [D0], CL
	MOV CL , 3D
	MOV [D1], CL
	MOV [D2], BL
	JMP END
INC:
	MOV BL, [04]
	ADD BL, 3
	MOV [04], BL
	JMP CONTINUE

END:
	END